# 日志和预测结果保存优化

## ✅ 已执行的修改

### 1. **train_image.py - 统一日志文件**

#### 添加内容：
- ✅ 新增 `training_log.txt` 统一日志文件
- ✅ 每个 epoch 的详细信息写入 txt（而不是分散在多个文件）
- ✅ 训练开始时写入配置信息
- ✅ 每个 epoch 完成后追加详细的训练和验证指标
- ✅ 训练完成后写入总结（最佳 F1、模型保存位置等）

#### 日志文件位置：
```
model_out/
├── training_log.txt        ← 所有 epoch 的详细日志（新增）
├── epoch_losses.csv        ← CSV 格式（保留用于数据分析）
├── batch_losses.csv        ← 每批次的损失
├── best.pth               ← 最佳模型
├── latest.pth             ← 最新模型
└── checkpoints/           ← 所有 epoch 的 checkpoints
    ├── epoch_1.pth
    ├── epoch_2.pth
    └── ...
```

#### training_log.txt 格式示例：
```
====================================================================================================
Swin-UNet 训练日志
====================================================================================================
训练配置:
  最大轮数: 100
  批大小: 4
  学习率: 0.001
  验证阈值: 0.2
  数据目录: ./data1
====================================================================================================

Epoch 1/100, Train Loss: 0.2500, Val Loss: 0.2300, Val IoU: 0.1234, Val F1: 0.2500, Precision: 0.3000, Recall: 0.2000
  Train Avg Loss: 0.250000
  Val Loss: 0.230000
  Val IoU: 0.123400
  Val F1: 0.250000
  Val Precision: 0.300000
  Val Recall: 0.200000
----------------------------------------------------------------------------------------------------

Epoch 2/100, Train Loss: 0.2300, Val Loss: 0.2100, Val IoU: 0.1345, Val F1: 0.2700, Precision: 0.3200, Recall: 0.2200
  ...

====================================================================================================
训练完成总结
====================================================================================================
总 Epochs: 100
最佳 F1 分数: 0.407919
最佳模型位置: ./model_out/best.pth
最新模型位置: ./model_out/latest.pth
所有 checkpoints 位置: ./model_out/checkpoints
====================================================================================================
```

---

### 2. **test_image.py - 自动保存预测掩码图**

#### 改动内容：
- ✅ 移除对 `--is_savenii` 参数的依赖
- ✅ **自动创建** `predictions/` 子目录
- ✅ **自动保存** NPY 格式（用于后续处理）
- ✅ **自动保存** PNG 格式（用于可视化查看）
- ✅ 新增 `test_results.txt` 日志文件

#### 预测结果目录结构：
```
model_out/
├── predictions/           ← 预测结果（自动创建，自动保存）
│   ├── image1_pred.npy   ← 二进制格式
│   ├── image1_pred.png   ← 可视化 PNG 图像
│   ├── image2_pred.npy
│   ├── image2_pred.png
│   └── ...
└── test_results.txt      ← 测试结果总结（新增）
```

#### test_results.txt 格式示例：
```
================================================================================
测试结果
================================================================================
模型路径: ./model_out/best.pth
阈值: 0.2
测试数据路径: ./data1
================================================================================
平均损失: 0.1058
IoU: 0.2993
F1: 0.4607
Precision: 0.3921
Recall: 0.5583
总测试样本: 1246
预测掩码保存位置: ./model_out/predictions
================================================================================
```

---

## 📊 优势对比

| 特性 | 原代码 | 新代码 |
|------|-------|-------|
| 日志文件 | 分散的 CSV | 统一的 txt（易于查看） |
| 预测掩码保存 | 需要 `--is_savenii` 参数 | **自动保存**（无需参数） |
| 预测图格式 | 仅 NPY（不可视化） | NPY + PNG（两种格式） |
| 测试结果记录 | 控制台输出（易丢失） | 自动保存到 txt |
| 训练结果记录 | 控制台输出（易丢失） | 自动保存到 txt |

---

## 🚀 使用方式

### 训练
```bash
python train_image.py --batch_size 4 --max_epochs 100

# 输出包含：
# - ./model_out/training_log.txt （所有 epoch 详细日志）
# - ./model_out/epoch_losses.csv （CSV 格式数据）
# - ./model_out/best.pth （最佳模型）
# - ./model_out/latest.pth （最新模型）
# - ./model_out/checkpoints/ （所有 epoch checkpoints）
```

### 测试
```bash
# 不需要 --is_savenii 参数，默认自动保存预测结果
python test_image.py --model_path ./model_out/best.pth --root_path ./data1

# 输出包含：
# - ./model_out/predictions/*.png （可视化预测掩码）
# - ./model_out/predictions/*.npy （二进制预测掩码）
# - ./model_out/test_results.txt （测试结果总结）
```

---

## ⚠️ 关于之前删除的结果

由于我之前执行了 `Remove-Item -Recurse -Force ./model_out`，之前 100 epochs 的训练结果无法恢复。

**建议**：重新执行训练，这次所有结果会完整地保存到 `training_log.txt` 中。

---

## 💾 数据备份建议

未来建议定期备份以下目录：
- `./model_out/best.pth` - 最佳模型
- `./model_out/training_log.txt` - 训练日志
- `./model_out/predictions/` - 预测结果
